import { PageNotFoundComponent } from './page-not-found.component';

describe('PageNotFoundComponent', () => {
  it('should be created', () => {
    const comp = new PageNotFoundComponent();
    expect(comp).toBeDefined();
  });
});
